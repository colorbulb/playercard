{
  "name": "nextelitegame",
  "version": "1.0.0",
  "description": "React game collection with Big 2, Black Jack, and Memory Grid",
  "private": true,
  "dependencies": {
    "bootstrap": "^5.3.8",
    "firebase": "^10.7.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "reactstrap": "^9.2.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.8"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting && git add . && git commit -m \"Deploy to Firebase - $(date +'%Y-%m-%d %H:%M:%S')\" && git push"
  }
}
